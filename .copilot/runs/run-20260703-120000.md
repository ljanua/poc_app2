# Run: Enable web server for E2E tests

- Request: enable solution via web server for end to end test
- Started: 2026-07-03 12:00:00

## Plan
- Inspect `package.json` and test config
- Ensure mockup server is reachable when running in container
- Update mock server binding
- Validate server files exist
- Record changes and provide test commands

## Implementation tasks
- Updated `scripts/serve-mockup.js` to bind to `0.0.0.0` (allow host/container access)

## Actions performed
- Verified `package.json` contains `serve:mockup` and `e2e:up` scripts
- Verified `playwright.config.js` uses webServer with `node scripts/serve-mockup.js`
- Confirmed mockup files exist under `docs/ux/mockup`
- Patched `scripts/serve-mockup.js` to default host `0.0.0.0`

## Files changed
- scripts/serve-mockup.js

## Verification
- Static inspection only. Server now binds to 0.0.0.0; to fully verify, run the server locally or in the devcontainer and run Playwright tests.

## Command outputs

- `npm install` completed (see terminal output). Some vulnerabilities found; not blocking for tests.
- `npx playwright test` failed due to Node.js version:

```
You are running Node.js 16.17.0.
Playwright requires Node.js 18 or higher.
Please update your version of Node.js.

Command exited with code 1
```

## Next steps
- Run tests inside the DevContainer (image uses Node 20) or upgrade local Node.js to >=18 and re-run `npx playwright test`.
- I can build and run the DevContainer and execute tests there if you want me to proceed.

## Blockers encountered
- Local Node.js is 16.17.0; Playwright requires Node.js >=18.
- Docker CLI is not available in this shell, so I cannot build/run the DevContainer image here.

## Options you can choose
- Open this repo in VS Code and "Reopen in Container" so the DevContainer with Node 20 runs the tests.
- Install/upgrade Node.js locally to >=18 (use nvm or the Node installer) then run:

```powershell
npm install
npx playwright install --with-deps
npx playwright test
```

- I can add a GitHub Actions workflow to run the Playwright suite in CI (I'll create it if you want).

Please tell me which option to take and I'll proceed.
## Final outcome
- Mockup server now binds to all interfaces enabling forwarded port access for E2E tests and devcontainer environments.

## Residual risks / next steps
- If running tests from host while server runs inside container, ensure `MOCKUP_HOST` and forwarded ports match.
- Optionally add an npm script to start both API and mock services, or a Docker compose for full stack.

