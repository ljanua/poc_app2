components:
  schemas:
    Club:
      type: object
      required: [id, name]
      properties:
        id:
          type: string
        name:
          type: string

    ClubListResponse:
      type: object
      required: [data]
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/Club'